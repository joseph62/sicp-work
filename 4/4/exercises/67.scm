; The functionality of loop detection could be accomplished by including
; a stack of pattern + frames from previous steps of the query. The system could
; check the current pattern and frame against the stack of patterns and frames that
; currently exist and stop the current query.
