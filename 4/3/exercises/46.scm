; If amb operands were evaluated in an order that was not
; left to right, the parsing program would break because the
; parsing noun and verb procedure rely on recursive usages of
; amb which build off of one another.
